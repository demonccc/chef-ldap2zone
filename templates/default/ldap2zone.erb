# File generated by Chef
# Don't edit it manually because all the changes will be overwritten

RUN_DEPLOY="false"

LDAP_URI="<%= @params['ldap']['uri'] %>"

BIND_DIR="<%= node['bind']['sysconfdir'] %>"

BIND_DATA="<%= @params['bind']['data'] %>"

TTL="<%= @params['ttl'] %>"

PREFIX="<%= @params['prefix'] %>"

<% unless @params['allow']['update'].empty? -%> 
ALLOW_UPDATE="<%= @params['allow']['update'].join(";") %>;"
<% end -%>

<% unless @params['allow']['transfer'].empty? -%>
ALLOW_TRANSFER="<%= @params['allow']['transfer'].join(";") %>;"
<% end -%>

<% if @params['allow']['notify'] -%>
ALLOW_NOTIFY="notify yes;"
<% end -%>
